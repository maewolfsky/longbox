%a.action{:href => "/"} Home
>
%a.action{:href => "/addPublisher"}Add a Publisher

%h1= "List of Publishers"
- @publishers.each do |p|
  %p 
    %a{ :href => "http://localhost:4567/publisher/#{p['name']}" } #{p['name']}
