// This will have an array passed into it somehow 
// then will iterate over it

%h1= "#{params[:name]}  #{params[:issuenumber]}"
- @issue.each do |issue|
  %h2 Number: #{issue[2]}
  %p Notes: #{issue[4]}