// This will have an array passed into it somehow 
// then will iterate over it

%h1= "List of Publishers"
- @publishers.each do |name|
  %p 
    %a{:href => "http://localhost:4567/publisher/#{name[0]}"} #{name[0]}
